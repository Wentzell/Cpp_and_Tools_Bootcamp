#include <gtest/gtest.h>

TEST(Test_Group, Test1) {
  EXPECT_TRUE(false);
  // XXX Do something
  // EXPECT_TRUE( .. );
  // EXPECT_FALSE( .. );
  // EXPECT_EQ( .. ); // a == b
  // EXPECT_NE( .. ); // a != b
  // EXPECT_LT( .. ); // a < b
  // EXPECT_LE( .. ); // a <= b
  // EXPECT_GT( .. ); // a > b
  // EXPECT_GE( .. ); // a >= b
}

TEST(Test_Group, Test2) {
  // XXX Do something
}
